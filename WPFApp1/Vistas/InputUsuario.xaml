<Window x:Class="WPFApp1.InputUsuario"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFApp1" WindowStartupLocation="CenterScreen" SizeToContent="WidthAndHeight"
        mc:Ignorable="d" FontFamily="Roboto" FontSize="16"
        Title="" Height="100" Width="300">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Iconos.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="10"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="10"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="{Binding TituloHint}"  FontSize="26" Margin="5" Grid.Row="0"/>
            <TextBox x:Name="Entrada"  Margin="5" Height="40" Text="{Binding EntradaTextoUsuario}" Grid.Row="1"/>
            <StackPanel Orientation="Horizontal" Margin="5" Grid.Row="2">
                <Button Content="Aceptar" Margin="0,0,5,0" Command="{Binding AceptarEntradaCommand}"/>
                <Button Content="Cancelar" Style="{StaticResource BotonSinEstilo}" Command="{Binding CancelarEntradaCommand}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
